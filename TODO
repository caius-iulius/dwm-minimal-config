-dwm:
    riparare la patch dwm-rounded-corners eliminando le impostazioni personalizzate
    creare una patch per le mie impostazioni
    risistema le patch in generale, fai una lista di quelle che sono state applicate
    la patch dwm-center non funziona per qualche motivo
-st:
    crea un'icona 32x32
-dmenu:
    per il momento è ok

-altro:
    aggiungi batteria, wireless e temperature alla barra di stato
    installa una suite per i media
    aggiungi un .zshrc base ai dotfiles
